---
phase: 01-security-foundation-keystones
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - cabofeira/src/data/seedUsers.js
  - cabofeira/src/pages/Login.jsx
  - cabofeira/src/i18n/en.json
  - cabofeira/src/i18n/pt-cv.json
autonomous: false
requirements: [SEC-04]
user_setup:
  - service: supabase
    why: "Delete the two demo Auth accounts (live in Supabase Auth, not in git)"
    dashboard_config:
      - task: "Delete admin@cabofeira.cv and user@cabofeira.cv after confirming a real admin account exists"
        location: "Supabase Dashboard -> Authentication -> Users"
must_haves:
  truths:
    - "Neither demo account (admin@cabofeira.cv, user@cabofeira.cv) can authenticate against prod Auth"
    - "The demo-credential strings admin123/user123 are gone from the working tree (seedUsers.js deleted, Login.jsx block removed, schema.sql block removed in Plan 01)"
    - "The auth.demoAccounts i18n key and the 'Demo' wording in featuredHint are removed from both en.json and pt-cv.json"
    - "admin123/user123 no longer appear anywhere in git history"
  artifacts:
    - path: "cabofeira/src/i18n/en.json"
      provides: "demo strings removed (bilingual parity preserved)"
    - path: "cabofeira/src/i18n/pt-cv.json"
      provides: "demo strings removed (bilingual parity preserved)"
  key_links:
    - from: "git history"
      to: "scrubbed credential strings"
      via: "git-filter-repo or BFG replace-text + force-push"
      pattern: "REMOVED"
---

<objective>
Complete SEC-04: remove the two demo Auth accounts from prod, scrub the demo-credential strings (`admin123`/`user123`) from the JS/JSON source files, and rewrite git history to purge the strings, then force-push. The `schema.sql` demo block was already removed in Plan 01; this plan handles the remaining files plus the destructive history rewrite. The credentials are low actual risk (they authenticate accounts being deleted), but SEC-04 explicitly requires history scrubbing — belt-and-suspenders per Claude's Discretion in CONTEXT.

Purpose: No demo/default account authenticates against prod and no demo credential remains in the repo or its history before launch.
Output: Deleted dead file, cleaned Login.jsx + bilingual i18n files, and a rewritten + force-pushed git history.
</objective>

<execution_context>
@E:/Projecten/Cabofeira/.claude/gsd-core/workflows/execute-plan.md
@E:/Projecten/Cabofeira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-foundation-keystones/01-RESEARCH.md
@.planning/phases/01-security-foundation-keystones/01-PATTERNS.md
@cabofeira/src/pages/Login.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub demo credentials from the working tree (files)</name>
  <files>cabofeira/src/data/seedUsers.js, cabofeira/src/pages/Login.jsx, cabofeira/src/i18n/en.json, cabofeira/src/i18n/pt-cv.json</files>
  <read_first>
    - cabofeira/src/pages/Login.jsx (commented demo block lines 87-101 — confirmed `admin123`/`user123` literals)
    - cabofeira/src/i18n/en.json (auth.demoAccounts ~line 221; featuredHint ~line 191 "(Demo only — no payment.)")
    - cabofeira/src/i18n/pt-cv.json (auth.demoAccounts ~line 221; featuredHint ~line 191 "(Demo — sem pagamento.)")
    - 01-RESEARCH.md Runtime State Inventory (credential locations + A1 seedUsers is dead code) + SEC-04 runbook step 3
    - 01-PATTERNS.md "SEC-04 credential scrub" table
  </read_first>
  <action>
    Before deleting, confirm seedUsers.js has no importer: grep the codebase for `seedUsers` and verify only self-references exist (RESEARCH A1). If an importer exists, STOP and flag — do not break a build. Otherwise:
    - Delete the whole file `cabofeira/src/data/seedUsers.js` (dead code carrying `admin123`/`user123`).
    - In `cabofeira/src/pages/Login.jsx`, delete the entire commented-out demo block (lines 87-101) including the `admin123`/`user123` literals and the surrounding `{/* ... */}`.
    - In `cabofeira/src/i18n/en.json`, remove the `auth.demoAccounts` key entirely and remove the "Demo" parenthetical from `featuredHint` (keep the rest of the featuredHint sentence intact).
    - In `cabofeira/src/i18n/pt-cv.json`, remove the matching `auth.demoAccounts` key and the matching "Demo" parenthetical from `featuredHint`.
    Keep both JSON files valid and in bilingual parity (the same keys must exist in both — you are removing the same key from each). Also check (per Claude's Discretion in CONTEXT) whether any live demo-login convenience UI references these keys; the Login.jsx block is commented-out, so removing the key is safe, but verify no other component reads `auth.demoAccounts`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); if(fs.existsSync('cabofeira/src/data/seedUsers.js')){console.error('seedUsers.js still exists');process.exit(1);} const login=fs.readFileSync('cabofeira/src/pages/Login.jsx','utf8'); ['admin123','user123','demoAccounts'].forEach(t=>{ if(login.includes(t)){console.error('Login.jsx still has '+t);process.exit(1);} }); for(const f of ['cabofeira/src/i18n/en.json','cabofeira/src/i18n/pt-cv.json']){ const j=JSON.parse(fs.readFileSync(f,'utf8')); if(JSON.stringify(j).includes('demoAccounts')){console.error(f+' still has demoAccounts');process.exit(1);} if(JSON.stringify(j).toLowerCase().includes('admin123')){console.error(f+' still has admin123');process.exit(1);} } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `seedUsers.js` is deleted (and had no importer).
    - `Login.jsx` contains no `admin123`/`user123`/`demoAccounts`.
    - Both `en.json` and `pt-cv.json` are valid JSON, contain no `auth.demoAccounts` key, no `admin123`, and the "Demo" parenthetical is gone from `featuredHint` in both — bilingual parity preserved.
  </acceptance_criteria>
  <done>All demo-credential strings removed from the working tree; bilingual i18n parity intact.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Delete demo Auth accounts and scrub git history</name>
  <action>Destructive ops step — see how-to-verify. Delete demo accounts first, then rewrite history with git-filter-repo/BFG and force-push.</action>
  <what-built>Working-tree scrub (Task 1) is staged/committed; the demo Auth accounts still live in Supabase Auth and the credential strings still live in git history.</what-built>
  <how-to-verify>
    This is the destructive ops half of SEC-04 — it deletes live accounts and rewrites shared git history, so it is [BLOCKING] and must be done by you in this order (do NOT reorder — neutralize the secret FIRST):

    1. CONFIRM a real (non-demo) admin account exists in `public.profiles` with role='admin'. Deleting the demo admin cascades to its profile/products — do not lock yourself out of admin.
    2. In Supabase Dashboard -> Authentication -> Users, DELETE `admin@cabofeira.cv` and `user@cabofeira.cv`. (This immediately neutralizes admin123/user123 — they now authenticate nothing.)
    3. Commit the Task 1 working-tree scrub if not already committed.
    4. Scrub history. Create a `replacements.txt` with lines `admin123==>***REMOVED***` and `user123==>***REMOVED***`, then run ONE of:
       - `git filter-repo --replace-text replacements.txt`  (requires `pip install git-filter-repo` first — not currently installed), OR
       - `java -jar bfg.jar --replace-text replacements.txt .git` then `git reflog expire --expire=now --all && git gc --prune=now --aggressive`  (Java 1.8 is installed; download bfg.jar).
    5. Verify the scrub: `git log -S admin123 --oneline` and `git log -S user123 --oneline` BOTH return nothing.
    6. `git push --force` on `main`. Since this is a small/solo repo, re-clone any other working copies afterward; never merge a pre-rewrite branch back (it would reintroduce the tainted history).

    Verified locations of the strings in history: commits 7bf50ac, 3c67242, 6849513, bfac3e5.
  </how-to-verify>
  <resume-signal>Type "scrubbed and pushed" once both `git log -S` searches are empty and the force-push succeeded, or describe what blocked you.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod Auth | Default/demo credentials must not authenticate any account. |
| git remote (history) | Leaked secret strings must not be retrievable from any commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | login with demo credentials admin@cabofeira.cv/admin123 | mitigate | delete both demo Auth accounts (BLOCKING ops); credentials authenticate nothing afterward (SEC-04) |
| T-05-02 | Information Disclosure | demo passwords recoverable from git history | mitigate | git-filter-repo/BFG replace-text + force-push; verified empty via `git log -S` (SEC-04) |
| T-05-03 | Elevation of Privilege | deleting demo admin removes the only admin profile | mitigate | step 1 confirms a real admin exists before deletion (RESEARCH A2) |
| T-05-04 | Tampering | tainted pre-rewrite branch merged back, reintroducing secrets | accept | small/solo repo; runbook instructs re-clone and not merging old branches |
| T-05-SC | Tampering | git-filter-repo install (pip) | mitigate | git-filter-repo is the GitHub-documented official tool (RESEARCH Package Legitimacy Audit — Approved); BFG fallback needs no install |
</threat_model>

<verification>
SEC-04 probe from 01-RESEARCH.md (line 538): attempting login `admin@cabofeira.cv/admin123` fails (account deleted); `git log -S admin123` is empty after the scrub. Both are confirmed in the BLOCKING checkpoint resume.
</verification>

<success_criteria>
- seedUsers.js deleted; Login.jsx and both i18n files free of demo credentials with bilingual parity.
- Both demo Auth accounts deleted (a real admin confirmed first).
- `git log -S admin123` and `git log -S user123` return nothing; history force-pushed.
</success_criteria>

<output>
Create `.planning/phases/01-security-foundation-keystones/01-05-SUMMARY.md` when done.
</output>
