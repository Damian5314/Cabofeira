---
phase: 01-security-foundation-keystones
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - .planning/phases/01-security-foundation-keystones/verification-probe.mjs
autonomous: false
requirements: [FND-01, FND-02, FND-03, SEC-01, SEC-02, SEC-03, SEC-04]
user_setup:
  - service: supabase
    why: "Apply the new/changed SQL manually (no migration runner) and run direct-API probes with a test account"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
    dashboard_config:
      - task: "Run schema.sql, security_guards.sql, notifications.sql, admin_audit_log.sql in this order in the SQL editor"
        location: "Supabase Dashboard -> SQL Editor"
must_haves:
  truths:
    - "All Phase 1 SQL is applied to the live Supabase project in the correct order"
    - "A normal authenticated account cannot change its own role or verified (probe confirms silent reset)"
    - "A normal account cannot reassign seller_id, flip featured/seller_verified, or set arbitrary views; insert is forced to safe defaults"
    - "increment_product_views() still increments views by 1 (D-20 probe passes)"
    - "anon sees only active products; owner/admin see their non-active rows"
    - "create_notification() row is visible only to its owner; log_admin_action() rejects non-admins and the audit table cannot be mutated"
  artifacts:
    - path: ".planning/phases/01-security-foundation-keystones/verification-probe.mjs"
      provides: "runnable supabase-js script encoding the RESEARCH probe table"
      contains: "increment_product_views"
  key_links:
    - from: "verification-probe.mjs"
      to: "live Supabase project"
      via: "supabase-js anon client signed in as a throwaway normal user"
      pattern: "createClient"
---

<objective>
Apply all Phase 1 SQL to the live Supabase project (manual, no migration runner) and prove the security boundary holds with a direct-API verification pass. This plan turns the authored SQL from Plans 01-03 into enforced behavior and validates every one of the 5 ROADMAP success criteria via the RESEARCH probe table — the only valid authorization-QA method for an RLS phase (UI testing cannot reach RLS holes).

Purpose: Confirm a normal authenticated account calling supabase-js/curl directly cannot escalate, steal, mass-assign, or read non-active listings, and that the keystones (status, notifications, audit log) behave as specified.
Output: A runnable `verification-probe.mjs` and a confirmed-applied database.
</objective>

<execution_context>
@E:/Projecten/Cabofeira/.claude/gsd-core/workflows/execute-plan.md
@E:/Projecten/Cabofeira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-foundation-keystones/01-RESEARCH.md
@cabofeira/supabase/schema.sql
@cabofeira/supabase/security_guards.sql
@cabofeira/supabase/notifications.sql
@cabofeira/supabase/admin_audit_log.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the direct-API verification probe script</name>
  <files>.planning/phases/01-security-foundation-keystones/verification-probe.mjs</files>
  <read_first>
    - 01-RESEARCH.md "Verification probes the planner MUST include" table (lines 526-538) + Pitfall 1 verification + "Validation Architecture" (probes runnable from a scratch node script using anon-key supabase-js as a throwaway normal user)
    - cabofeira/src/lib/supabase.js (existing supabase client creation pattern / env var names)
  </read_first>
  <action>
    Create `.planning/phases/01-security-foundation-keystones/verification-probe.mjs` — an ESM Node script using `@supabase/supabase-js` `createClient(process.env.SUPABASE_URL, process.env.SUPABASE_ANON_KEY)`. The script signs in as a throwaway NORMAL (non-admin) test user (email/password passed via env: TEST_EMAIL, TEST_PASSWORD; print a clear instruction if unset). It then runs each probe from the RESEARCH probe table and asserts the expected outcome, printing PASS/FAIL per probe and exiting non-zero on any FAIL:
    - SEC-01: `update profiles set role='admin' where id=<self>` → re-read, assert role still 'user'; same for `verified=true` → still false.
    - SEC-02: on a product the test user owns, `update products set seller_id=<other>` → assert unchanged; `update ... set featured=true, seller_verified=true, views=9999` → assert all pinned to OLD.
    - SEC-03: `insert into products (...featured=true, seller_verified=true, views=50, status='hidden'...)` → re-read, assert stored featured=false, seller_verified=false, views=0, status='active'.
    - D-20: `rpc('increment_product_views', {p_id:<own>})` then re-read → assert views incremented by exactly 1.
    - FND-01: anon (a second unauthenticated client) `select * from products` → assert every returned row has status='active'; owner `select` filtered to own seller_id → assert own non-active rows ARE returned.
    - FND-02: `rpc('create_notification', {p_user_id:<self>, p_type:'system', p_title:'t'})` → owner select returns the row; a second normal user select returns none.
    - FND-03: non-admin `rpc('log_admin_action', {p_action:'x'})` → assert error 'Not authorized'; direct `update`/`delete` on admin_audit_log → assert denied.
    - SEC-04 (ROADMAP success criterion 5): using a fresh client, attempt BOTH demo-account logins — `supabase.auth.signInWithPassword({ email:'admin@cabofeira.cv', password:'admin123' })` AND `supabase.auth.signInWithPassword({ email:'user@cabofeira.cv', password:'user123' })` — and assert EACH returns a non-null `error` and a null `data.session` (login MUST fail because the accounts were deleted in Plan 05). This is the runnable check for ROADMAP success criterion 5.
    Use the `{ data, error }` supabase-js return shape and the `seller_id`/`status` column names confirmed in schema.sql. The script must be safe to re-run (it creates and can clean up its own throwaway product rows). Do NOT hardcode any credentials in the file (the two demo emails/passwords are the strings being PROVEN dead, not live secrets). NOTE on ordering: this probe is authored here in Wave 2 but is RUN BY THE HUMAN LAST — after both the SQL apply (Plan 04, Task 2) AND the demo-account deletion + history scrub (Plan 05) are complete. Authoring in Wave 2 while executing last is fine; the SEC-04 assertions only pass once the accounts no longer exist.
  </action>
  <verify>
    <automated>node --check .planning/phases/01-security-foundation-keystones/verification-probe.mjs && node -e "const s=require('fs').readFileSync('.planning/phases/01-security-foundation-keystones/verification-probe.mjs','utf8'); ['createClient','increment_product_views','create_notification','log_admin_action','seller_id','status','signInWithPassword'].forEach(t=>{ if(!s.includes(t)){ console.error('MISSING: '+t); process.exit(1);} }); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `verification-probe.mjs` parses (`node --check` passes) and encodes every probe from the RESEARCH table (SEC-01/02/03, D-20, FND-01/02/03).
    - It reads URL/key/credentials from env, hardcodes nothing, prints PASS/FAIL per probe, exits non-zero on any FAIL.
    - verification-probe.mjs asserts both demo-account logins fail (error non-null, session null), covering ROADMAP success criterion 5 / SEC-04.
  </acceptance_criteria>
  <done>A runnable, credential-free probe script encoding all 5 ROADMAP success criteria exists.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply all Phase 1 SQL in Supabase in the correct order</name>
  <action>Manual apply step — see how-to-verify. No automated db push (CLAUDE.md: backend changes applied by hand in Supabase).</action>
  <what-built>SQL authored in Plans 01-03: schema.sql edits (status column, SELECT policy, WITH CHECK, demo block removed), security_guards.sql (three guard triggers), notifications.sql, admin_audit_log.sql.</what-built>
  <how-to-verify>
    There is no migration runner — you must apply the SQL manually in the Supabase SQL editor, IN THIS ORDER (order matters: the status column must exist before its SELECT policy; guards reference products/profiles):
    1. Open Supabase Dashboard -> SQL Editor for the CaboFeira project.
    2. Run `cabofeira/supabase/schema.sql` (adds products.status, rewrites the products SELECT policy, adds WITH CHECK to the self-update policies). It is idempotent — safe even though the base schema already exists.
    3. Run `cabofeira/supabase/security_guards.sql` (installs the three BEFORE-trigger guards).
    4. Run `cabofeira/supabase/notifications.sql`.
    5. Run `cabofeira/supabase/admin_audit_log.sql`.
    6. Confirm no SQL errors were returned for any file.
    This is a [BLOCKING] manual step because the project applies all backend changes by hand in Supabase (CLAUDE.md constraint) — do not attempt an automated `supabase db push`.
  </how-to-verify>
  <resume-signal>Type "applied" once all four files ran without error, or paste any SQL error returned.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Run the direct-API verification probe against the applied DB</name>
  <action>Run verification-probe.mjs — see how-to-verify. Every probe must PASS.</action>
  <what-built>The verification-probe.mjs script (Task 1), to be run against the now-applied database.</what-built>
  <how-to-verify>
    1. Ensure a throwaway NORMAL (non-admin) test account exists in Supabase Auth (create one via the app signup or the dashboard). Also have a second normal account for the cross-user notification check.
    2. Set env vars: SUPABASE_URL, SUPABASE_ANON_KEY (Dashboard -> Settings -> API), TEST_EMAIL, TEST_PASSWORD (the throwaway account).
    3. From the repo root run: `node .planning/phases/01-security-foundation-keystones/verification-probe.mjs`
    4. Confirm EVERY probe prints PASS and the process exits 0. Expected results map to the RESEARCH probe table: role/verified silently reset, seller_id/featured/seller_verified/arbitrary-views pinned, insert forced to safe defaults + status='active', `increment_product_views` bumps views by 1, anon sees only active rows, create_notification visible only to owner, log_admin_action rejects the non-admin and the audit table cannot be mutated.
    5. If any probe FAILs, report which one — that is a real RLS hole and must be fixed before the phase can complete.
  </how-to-verify>
  <resume-signal>Type "all probes pass" or paste the failing probe output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(supabase-js anon JWT) → Postgres | The probe IS the adversary: a normal authenticated account attempting every forbidden operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation/Tampering/Info-Disclosure | all SEC-01/02/03 + FND-01 holes | mitigate | direct-API probe pass confirms each guard/policy from Plans 01-03 actually blocks the attack against the live DB |
| T-04-02 | Tampering | SQL applied in wrong order leaves a hole (e.g., SELECT policy before status column) | mitigate | BLOCKING ordered manual-apply checkpoint enumerates the exact run order |
| T-04-03 | Repudiation | audit table mutable after apply | mitigate | FND-03 probe attempts update/delete on admin_audit_log and asserts denial |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Probe uses the already-present @supabase/supabase-js dependency; no new install. If a global install is needed, run the script from cabofeira/ where the dep already exists. |
</threat_model>

<verification>
This plan IS the phase verification step. The probe script maps 1:1 to the RESEARCH probe table (01-RESEARCH.md lines 526-538) and the 5 ROADMAP success criteria. The phase is not complete until every probe passes against the applied database.
</verification>

<success_criteria>
- All four SQL files applied in order with no errors (BLOCKING checkpoint confirmed).
- verification-probe.mjs runs green: every probe PASS, exit 0.
- All 5 ROADMAP success criteria demonstrably hold against the live database.
</success_criteria>

<output>
Create `.planning/phases/01-security-foundation-keystones/01-04-SUMMARY.md` when done, recording the probe results.
</output>
