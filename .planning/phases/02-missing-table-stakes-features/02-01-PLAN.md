---
phase: 02-missing-table-stakes-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cabofeira/supabase/blocked_users.sql
  - cabofeira/supabase/messages_block_rls.sql
  - cabofeira/supabase/reports_reason.sql
  - cabofeira/supabase/products_sold_visibility.sql
  - cabofeira/supabase/product_post_log.sql
  - cabofeira/supabase/new_message_notification.sql
  - cabofeira/supabase/functions/verify-turnstile/index.ts
autonomous: true
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-05, FEAT-08, ABUSE-01, ABUSE-02]
must_haves:
  truths:
    - "Six per-concern SQL files exist in cabofeira/supabase/, each idempotent and SQL-editor-pasteable"
    - "blocked_users table + is_blocked_pair() helper are defined before any file that references them"
    - "Messaging RLS rewrite excludes blocked pairs server-side (D-09 #2)"
    - "Rate-limit guard counts an append-only log that survives hard product deletion (D-05)"
    - "verify-turnstile Edge Function source verifies the token server-side with the secret never in the client"
  artifacts:
    - path: "cabofeira/supabase/blocked_users.sql"
      provides: "blocked_users table + owner RLS + is_blocked_pair(a,b) helper"
      contains: "is_blocked_pair"
    - path: "cabofeira/supabase/messages_block_rls.sql"
      provides: "conversations/messages SELECT + messages INSERT policies excluding blocked pairs"
      contains: "is_blocked_pair"
    - path: "cabofeira/supabase/reports_reason.sql"
      provides: "reports.reason CHECK + unique(reporter_id,product_id) + backfill"
      contains: "reports_reason_check"
    - path: "cabofeira/supabase/products_sold_visibility.sql"
      provides: "relaxed products SELECT policy exposing active+sold"
      contains: "status in ('active', 'sold')"
    - path: "cabofeira/supabase/product_post_log.sql"
      provides: "append-only log + enforce_post_rate_limit BEFORE INSERT + log_product_post AFTER INSERT"
      contains: "enforce_post_rate_limit"
    - path: "cabofeira/supabase/new_message_notification.sql"
      provides: "notify_new_message AFTER INSERT trigger calling create_notification"
      contains: "notify_new_message"
    - path: "cabofeira/supabase/functions/verify-turnstile/index.ts"
      provides: "Deno siteverify Edge Function"
      contains: "siteverify"
  key_links:
    - from: "messages_block_rls.sql"
      to: "blocked_users.sql is_blocked_pair"
      via: "policy using/with check calls is_blocked_pair"
      pattern: "is_blocked_pair"
    - from: "new_message_notification.sql"
      to: "notifications.sql create_notification"
      via: "perform create_notification in trigger fn"
      pattern: "create_notification"
---

<objective>
Author every new backend artifact this phase needs as standalone, idempotent, SQL-editor-pasteable files (and the one Edge Function source) WITHOUT applying anything to the live database. This plan is pure authoring; the human apply/provision gate is Plan 02-02.

Purpose: Front-load all SQL/Edge authoring into one peer-reviewable plan so the manual-apply gate (02-02) can paste files in a strict, known order. Separating authoring from applying mirrors Phase 1's 01-04 pattern (SQL applied manually, no migration runner).
Output: 6 new SQL files in cabofeira/supabase/ + 1 Edge Function at cabofeira/supabase/functions/verify-turnstile/index.ts.
</objective>

<execution_context>
@E:/Projecten/Cabofeira/.claude/gsd-core/workflows/execute-plan.md
@E:/Projecten/Cabofeira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-missing-table-stakes-features/02-CONTEXT.md
@.planning/phases/02-missing-table-stakes-features/02-RESEARCH.md
@.planning/phases/02-missing-table-stakes-features/02-PATTERNS.md
@.planning/phases/01-security-foundation-keystones/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Block-user + messaging-RLS + new-message-notification SQL (dependency chain)</name>
  <files>cabofeira/supabase/blocked_users.sql, cabofeira/supabase/messages_block_rls.sql, cabofeira/supabase/new_message_notification.sql</files>
  <read_first>
    - cabofeira/supabase/reports.sql (table + idempotent owner-RLS convention to mirror for blocked_users)
    - cabofeira/supabase/admin_audit_log.sql (SECURITY DEFINER + set search_path=public fn convention)
    - cabofeira/supabase/messages.sql (existing conversations/messages policies L58-87 to rewrite; bump_conversation_last_message AFTER INSERT trigger L32-49)
    - cabofeira/supabase/notifications.sql (create_notification signature + cross-user gate)
    - .planning/phases/02-missing-table-stakes-features/02-RESEARCH.md (§"blocked_users table + helper", §"Block-aware messaging RLS", §"Pattern 3")
    - .planning/phases/02-missing-table-stakes-features/02-PATTERNS.md (blocked_users.sql / messages_block_rls.sql / new_message_notification.sql assignments)
  </read_first>
  <action>
    Create three SQL files (per FEAT-05 / D-07/D-08/D-09 / FEAT-08 / D-16). Each opens with a "-- Run this in the Supabase SQL editor" banner (manual apply, no migration runner). Use idempotent drop-then-create policy blocks and create-if-not-exists.

    blocked_users.sql: create table public.blocked_users (blocker_id, blocked_id, created_at, primary key (blocker_id,blocked_id), check (blocker_id <> blocked_id)), both FK to profiles(id) on delete cascade. Enable RLS. Three owner-scoped policies keyed on auth.uid()=blocker_id: blocked_select_own, blocked_insert_own, blocked_delete_own (mirror reports.sql owner policies). Add is_blocked_pair(a uuid, b uuid) returns boolean language sql stable security definer set search_path=public returning exists(select 1 from blocked_users where (blocker_id=a and blocked_id=b) or (blocker_id=b and blocked_id=a)). NO realtime publication (block list is not live-subscribed). Use the exact RESEARCH §"blocked_users table + helper" SQL. This file MUST be ordered FIRST.

    messages_block_rls.sql: re-issue all three existing policies with drop-policy-if-exists then create (idempotent, same convention as messages.sql L58-68): conversations_select_participant (participant AND not is_blocked_pair(buyer_id, seller_id)); messages_select_participant (participant via conversations subquery AND not is_blocked_pair on that conversation's pair); messages_insert_participant (existing participant with-check AND not is_blocked_pair). The INSERT with-check failing is what makes a blocked send silently not deliver (D-08). Use the exact RESEARCH §"Block-aware messaging RLS" SQL. This is the HIGH-severity server-side block-enforcement boundary (D-09 #2) — encode it server-side, never client-only.

    new_message_notification.sql: notify_new_message() returns trigger, security definer set search_path=public (Pitfall 3 — definer context satisfies create_notification()'s cross-user gate). Resolve v_recipient as the non-sender participant from conversations; if is_blocked_pair(new.sender_id, v_recipient) then return new (no bell for blocked pairs — D-08/D-10); select v_sender_name from profiles; perform create_notification(v_recipient, 'new_message', v_sender_name, left(new.body,140), jsonb_build_object('conversation_id', new.conversation_id, 'sender_id', new.sender_id), '/messages'). drop trigger if exists trg_notify_new_message; create trigger after insert on public.messages for each row. Use the exact RESEARCH §"Pattern 3" SQL.

    Apply-order header comment in blocked_users.sql noting it must be applied before messages_block_rls.sql and new_message_notification.sql (both call is_blocked_pair).
  </action>
  <verify>
    <automated>MISSING — no test suite this milestone (nyquist_validation=false). Static check instead: grep -l "is_blocked_pair" cabofeira/supabase/blocked_users.sql cabofeira/supabase/messages_block_rls.sql cabofeira/supabase/new_message_notification.sql returns all three; grep "security definer" cabofeira/supabase/blocked_users.sql and new_message_notification.sql each match.</automated>
    <human-check>Read each file; confirm it is self-contained, idempotent, and matches the RESEARCH snippets; confirm the three SQL files declare the FIRST/AFTER apply order in comments.</human-check>
  </verify>
  <acceptance_criteria>
    blocked_users.sql defines the table, three owner policies, and is_blocked_pair(); messages_block_rls.sql rewrites all three messaging policies appending "not is_blocked_pair(...)"; new_message_notification.sql defines a SECURITY DEFINER AFTER-INSERT trigger that short-circuits on blocked pairs and calls create_notification. All three are idempotent and reference is_blocked_pair from blocked_users.sql.
  </acceptance_criteria>
  <done>Three SQL files written, dependency-ordered, idempotent, matching RESEARCH snippets; is_blocked_pair defined once and referenced by the other two.</done>
</task>

<task type="auto">
  <name>Task 2: Reports reason + products sold-visibility + rate-limit SQL</name>
  <files>cabofeira/supabase/reports_reason.sql, cabofeira/supabase/products_sold_visibility.sql, cabofeira/supabase/product_post_log.sql</files>
  <read_first>
    - cabofeira/supabase/reports.sql (target table for reason CHECK + unique)
    - cabofeira/supabase/notifications.sql (text + CHECK enum convention, L11)
    - cabofeira/supabase/schema.sql (products_select_active_or_owner_or_admin policy ~L155 — the exact policy being relaxed)
    - cabofeira/supabase/security_guards.sql (BEFORE INSERT guard pattern L84-107; confirm guard_products_insert pins status='active' for non-admins and guard_products_update does NOT pin status)
    - cabofeira/supabase/messages.sql (AFTER INSERT trigger pattern L46-49)
    - .planning/phases/02-missing-table-stakes-features/02-RESEARCH.md (§"Rate limit, hard-delete-proof", Pitfall 1, Pitfall 2, Pitfall 4)
  </read_first>
  <action>
    Create three SQL files (per FEAT-02/FEAT-03 / D-20/D-21, FEAT-01 / D-13/D-14, ABUSE-01 / D-04/D-05). Each opens with the "-- Run this in the Supabase SQL editor" banner.

    reports_reason.sql (FEAT-02/FEAT-03, D-20/D-21): FIRST line is a defensive backfill for Pitfall 4 — update public.reports set reason='other' where reason not in ('scam','spam','prohibited','already_sold','duplicate','other'); — so the CHECK won't fail on existing free-text rows. Then alter table public.reports add constraint reports_reason_check check (reason in ('scam','spam','prohibited','already_sold','duplicate','other')); and add constraint reports_reporter_product_uniq unique (reporter_id, product_id). Wrap constraint adds in a do-block or use "if not exists"-style guards (drop constraint if exists then add) so the file is idempotent. Add a comment noting the pre-apply count check step (the apply gate runs select count(*) from public.reports where reason not in (...) before applying — see 02-02).

    products_sold_visibility.sql (FEAT-01, D-14): drop policy if exists "products_select_active_or_owner_or_admin" on public.products; then recreate with using (status in ('active','sold') or auth.uid() = seller_id or public.is_admin()). Keep expired/hidden owner/admin-only (do NOT widen to all statuses — Information Disclosure threat). Add a prominent comment: "MUST be applied together with the ProductsContext .eq('status','active') feed/search edits in Plan 02-03 — otherwise Home/Search leak sold ads (Pitfall 1)." Note the Phase-1 guard triggers are UNCHANGED (guard_products_insert pins status='active' for non-admins so sold is reachable only via UPDATE; guard_products_update does not pin status so mark-as-sold works).

    product_post_log.sql (ABUSE-01, D-04/D-05): create table public.product_post_log (id uuid primary key default gen_random_uuid(), user_id uuid not null, created_at timestamptz not null default now()) — user_id is NOT a cascading FK to products (Pitfall 2: must survive hard removeProduct DELETE). Index on (user_id, created_at desc). Enable RLS; NO client policies (rows enter only via the definer AFTER-INSERT trigger). enforce_post_rate_limit() BEFORE INSERT, security definer set search_path=public: if public.is_admin() then return new (admins exempt); count product_post_log where user_id=new.seller_id and created_at > now() - interval '1 hour'; if >= 10 raise exception 'RATE_LIMIT' using errcode='P0001', detail='ad_post_rate_limit'. log_product_post() AFTER INSERT, security definer set search_path=public: insert into product_post_log(user_id) values (new.seller_id). Create both triggers on public.products (drop-if-exists first). Use the exact RESEARCH §"Rate limit, hard-delete-proof" SQL. DIVERGENCE NOTE in a comment: unlike Phase-1 guards' silent reset (D-19), this guard RAISES — correct/intended for D-06 (block + friendly localized message); the client maps the coded error in Plan 02-07.
  </action>
  <verify>
    <automated>MISSING — no test suite. Static check: grep "reports_reason_check" cabofeira/supabase/reports_reason.sql; grep "status in ('active', 'sold')" -i cabofeira/supabase/products_sold_visibility.sql (allow spacing variance); grep "ad_post_rate_limit" cabofeira/supabase/product_post_log.sql; grep -c "on delete cascade" cabofeira/supabase/product_post_log.sql returns 0 (log must NOT cascade-delete with products).</automated>
    <human-check>Confirm reports_reason.sql backfills before adding the CHECK; products_sold_visibility.sql relaxes to active+sold only (not all statuses) and carries the paired-edit warning; product_post_log.sql user_id has no cascading FK and the guard raises P0001.</human-check>
  </verify>
  <acceptance_criteria>
    reports_reason.sql backfills then adds the reason CHECK + (reporter_id,product_id) unique idempotently; products_sold_visibility.sql relaxes SELECT to active+sold while keeping expired/hidden private and warns about the paired ProductsContext edit; product_post_log.sql defines an append-only non-cascading log + BEFORE-INSERT raising guard (>=10/hour, admins exempt) + AFTER-INSERT log writer.
  </acceptance_criteria>
  <done>Three SQL files written, idempotent, matching RESEARCH; no cascading FK on the log; SELECT relax limited to active+sold.</done>
</task>

<task type="auto">
  <name>Task 3: verify-turnstile Edge Function source</name>
  <files>cabofeira/supabase/functions/verify-turnstile/index.ts</files>
  <read_first>
    - .planning/phases/02-missing-table-stakes-features/02-RESEARCH.md (§"Edge Function: verify-turnstile", Assumption A6, Security Domain "Secret key leaked into client bundle")
    - .planning/phases/02-missing-table-stakes-features/02-PATTERNS.md (§"No Analog Found" — Edge Function notes)
  </read_first>
  <action>
    Create the Deno Edge Function source per ABUSE-02 / D-02 (posting CAPTCHA must be verified server-side; it is NOT an auth endpoint). Use the exact RESEARCH §"Edge Function: verify-turnstile" snippet: Deno.serve handler that reads { token } from the JSON body, builds FormData with secret=Deno.env.get("CLOUDFLARE_SECRET_KEY"), response=token, optional remoteip from CF-Connecting-IP, POSTs to https://challenges.cloudflare.com/turnstile/v0/siteverify, and returns { success: outcome.success === true } with status 200 on success / 403 on failure. The secret comes ONLY from Deno.env (set via `supabase secrets set CLOUDFLARE_SECRET_KEY=...` in the apply gate) — it MUST NOT use a REACT_APP_ prefix and MUST NEVER appear in client code (Assumption A6 / Information Disclosure threat). Add a top-of-file comment: deployed via Supabase CLI/dashboard (NOT the SQL editor); availability is verified in Plan 02-02 (Open Q1). Add minimal CORS handling consistent with the documented Supabase example so the browser invoke from PostAd works.
  </action>
  <verify>
    <automated>MISSING — no test suite. Static check: grep "siteverify" cabofeira/supabase/functions/verify-turnstile/index.ts; grep "CLOUDFLARE_SECRET_KEY" cabofeira/supabase/functions/verify-turnstile/index.ts; grep -c "REACT_APP" cabofeira/supabase/functions/verify-turnstile/index.ts returns 0 (secret must never use the client prefix).</automated>
    <human-check>Confirm the secret is read only from Deno.env, the function returns a boolean success, and a comment flags CLI/dashboard deploy + availability verification in 02-02.</human-check>
  </verify>
  <acceptance_criteria>
    index.ts is a Deno.serve siteverify handler reading the secret from Deno.env.get("CLOUDFLARE_SECRET_KEY"), returning {success:boolean}; no REACT_APP_ reference; comment documents CLI deploy + 02-02 availability check.
  </acceptance_criteria>
  <done>Edge Function source written matching the documented Supabase Turnstile example; secret never in client scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (anon JWT) → Postgres | All supabase-js calls; the SQL authored here is the enforcement layer (Phase-4 SEC-09 probes it raw) |
| client → Edge Function → Cloudflare siteverify | Posting CAPTCHA verification; the secret lives only server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / DoS | product_post_log rate limit | mitigate | DB-enforced enforce_post_rate_limit() BEFORE INSERT over an append-only, non-cascading log (survives hard delete) — never a client check (HIGH; D-05) |
| T-02-02 | Spoofing / Tampering | messages INSERT bypassing block | mitigate | messages_insert_participant with-check includes `not is_blocked_pair(...)` — server-side (HIGH; D-09 #2) |
| T-02-03 | Information Disclosure | reading blocked user's messages | mitigate | conversations/messages SELECT policies exclude blocked pairs |
| T-02-04 | Information Disclosure | products SELECT relax exposing hidden/expired | mitigate | relax to `status in ('active','sold')` ONLY; expired/hidden stay owner/admin (D-14) |
| T-02-05 | Spoofing | forging a notification to a victim's bell | mitigate | only notify_new_message (SECURITY DEFINER) calls create_notification; client has no INSERT path (Phase-1 gate) |
| T-02-06 | Information Disclosure | Turnstile secret leaked into client bundle | mitigate | CLOUDFLARE_SECRET_KEY lives only in Edge Function env (never REACT_APP_*) |
| T-02-SC | Tampering | npm/Edge deploy supply chain | accept (authored here, applied in 02-02) | @marsidev/react-turnstile install + Edge deploy are gated by the blocking-human checkpoint in Plan 02-02 |
</threat_model>

<verification>
- All six SQL files + the Edge Function exist under cabofeira/supabase/.
- is_blocked_pair is defined in blocked_users.sql and referenced by messages_block_rls.sql and new_message_notification.sql.
- products_sold_visibility.sql relaxes SELECT to active+sold only.
- product_post_log.sql has no cascading FK and raises P0001 on the 11th post/hour.
- No REACT_APP_ reference in the Edge Function.
- Nothing is applied to the live database in this plan (authoring only).
</verification>

<success_criteria>
Six idempotent, dependency-ordered SQL files and one Edge Function source are authored, each grounded in the RESEARCH snippets and PATTERNS analogs, ready for the manual apply/provision gate in Plan 02-02.
</success_criteria>

<output>
Create `.planning/phases/02-missing-table-stakes-features/02-01-SUMMARY.md` when done.
</output>
