---
phase: 02-missing-table-stakes-features
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - cabofeira/.env.example
autonomous: false
requirements: [FEAT-01, FEAT-02, FEAT-03, FEAT-05, FEAT-08, ABUSE-01, ABUSE-02]
user_setup:
  - service: cloudflare-turnstile
    why: "CAPTCHA on signup + posting (ABUSE-02, D-01)"
    env_vars:
      - name: REACT_APP_TURNSTILE_SITE_KEY
        source: "Cloudflare Dashboard -> Turnstile -> add widget -> Site Key (public, safe in client bundle)"
      - name: CLOUDFLARE_SECRET_KEY
        source: "Cloudflare Dashboard -> Turnstile -> widget -> Secret Key (server-only; set via `supabase secrets set`, NEVER REACT_APP_*)"
    dashboard_config:
      - task: "Enable native CAPTCHA (Turnstile provider) for Auth signup"
        location: "Supabase Dashboard -> Authentication -> Attack Protection / CAPTCHA -> select Turnstile + paste secret"
      - task: "Deploy the verify-turnstile Edge Function and set its secret"
        location: "supabase CLI: `supabase functions deploy verify-turnstile` + `supabase secrets set CLOUDFLARE_SECRET_KEY=...`"
must_haves:
  truths:
    - "All six Phase-2 SQL files are applied to the live Supabase DB in dependency order"
    - "Cloudflare Turnstile site key + secret key are provisioned"
    - "verify-turnstile Edge Function is deployed (or the pg_net fallback decision is recorded)"
    - "Supabase Auth native CAPTCHA is enabled for signup"
    - "@marsidev/react-turnstile@1.5.2 is installed"
  artifacts:
    - path: "cabofeira/.env.example"
      provides: "Documented REACT_APP_TURNSTILE_SITE_KEY placeholder (secret intentionally absent)"
      contains: "REACT_APP_TURNSTILE_SITE_KEY"
  key_links:
    - from: "live Supabase DB"
      to: "Plan 02-01 SQL files"
      via: "manual paste in the SQL editor, in order"
      pattern: "applied"
---

<objective>
The single human gate for this phase. A person applies the six Plan 02-01 SQL files to live Supabase in strict dependency order, provisions Cloudflare Turnstile keys, deploys (or decides the fallback for) the verify-turnstile Edge Function, toggles native Auth CAPTCHA, and installs the one new npm dependency. Every downstream UI plan (02-03 .. 02-08) depends on these backend artifacts being live.

Purpose: There is no migration runner (project constraint) — SQL ships as files applied manually in the Supabase SQL editor, exactly as Phase 1's 01-04 did. Turnstile keys, the Edge Function, the Auth toggle, and the npm install are all things Claude cannot do without human credentials/dashboards.
Output: Live DB with all Phase-2 schema applied; provisioned CAPTCHA infra; installed Turnstile widget; updated .env.example.
</objective>

<execution_context>
@E:/Projecten/Cabofeira/.claude/gsd-core/workflows/execute-plan.md
@E:/Projecten/Cabofeira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-missing-table-stakes-features/02-RESEARCH.md
@.planning/phases/02-missing-table-stakes-features/02-CONTEXT.md
@cabofeira/supabase/blocked_users.sql
@cabofeira/supabase/messages_block_rls.sql
@cabofeira/supabase/reports_reason.sql
@cabofeira/supabase/products_sold_visibility.sql
@cabofeira/supabase/product_post_log.sql
@cabofeira/supabase/new_message_notification.sql
@cabofeira/supabase/functions/verify-turnstile/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @marsidev/react-turnstile + document env vars</name>
  <files>cabofeira/.env.example</files>
  <read_first>
    - cabofeira/package.json (confirm React 19 + supabase-js versions; no existing turnstile dep)
    - cabofeira/src/lib/supabase.js (REACT_APP_* env convention)
    - .planning/phases/02-missing-table-stakes-features/02-RESEARCH.md (§Package Legitimacy Audit, Assumption A6)
  </read_first>
  <action>
    Per ABUSE-02 / D-01. The package @marsidev/react-turnstile@1.5.2 is [ASSUMED] (slopcheck could not run) but carries strong manual signals (1.58M dl/wk, Cloudflare-recommended, real repo, no postinstall) per RESEARCH §Package Legitimacy Audit. The legitimacy confirmation is handled by the blocking-human checkpoint in Task 2; once approved, run `npm install @marsidev/react-turnstile@1.5.2` from the cabofeira/ directory.

    Update cabofeira/.env.example: add REACT_APP_TURNSTILE_SITE_KEY (public site key — safe in the client bundle, follows the REACT_APP_ convention). Add a comment that CLOUDFLARE_SECRET_KEY is the Edge Function secret and MUST NOT be added here or anywhere with a REACT_APP_ prefix (it is set via `supabase secrets set`). If cabofeira/.env.example does not exist, create it mirroring the existing REACT_APP_SUPABASE_* keys used in lib/supabase.js.
  </action>
  <verify>
    <automated>cd cabofeira && npm ls @marsidev/react-turnstile (resolves to 1.5.2); grep "REACT_APP_TURNSTILE_SITE_KEY" cabofeira/.env.example.</automated>
  </verify>
  <acceptance_criteria>@marsidev/react-turnstile@1.5.2 is in package.json/node_modules; .env.example documents the public site key and warns the secret is server-only.</acceptance_criteria>
  <done>Dependency installed (after checkpoint approval); env example documents the site key.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply SQL in order + provision CAPTCHA infra (human gate)</name>
  <action>Human-only apply/provision gate — Claude cannot perform these dashboard/credential steps. Follow how-to-verify exactly; do not proceed past this task until the resume signal is given.</action>
  <what-built>Plan 02-01 authored six SQL files + the verify-turnstile Edge Function. Task 1 documented the env vars. This checkpoint is the human apply/provision gate — these steps require Supabase/Cloudflare dashboards and credentials Claude does not have.</what-built>
  <how-to-verify>
    1. PACKAGE LEGITIMACY (light confirm per RESEARCH A4): @marsidev/react-turnstile is [ASSUMED]. Confirm it is the real package at https://www.npmjs.com/package/@marsidev/react-turnstile (1.5.2, repo github.com/marsidev/react-turnstile, no postinstall). Approve the `npm install` from Task 1, or reject if anything looks off.
    2. PRE-APPLY CHECK (Pitfall 4): in the Supabase SQL editor run `select count(*) from public.reports where reason not in ('scam','spam','prohibited','already_sold','duplicate','other');`. If >0, the backfill line in reports_reason.sql will normalize them to 'other' on apply (confirm that is acceptable).
    3. APPLY SQL IN THIS EXACT ORDER (paste each file into the SQL editor, run, confirm no error):
       a) blocked_users.sql  (defines is_blocked_pair — must be first)
       b) messages_block_rls.sql  (references is_blocked_pair)
       c) new_message_notification.sql  (references is_blocked_pair + create_notification)
       d) reports_reason.sql  (backfill THEN CHECK + unique)
       e) products_sold_visibility.sql  (SELECT relax — pairs with Plan 02-03 app filter)
       f) product_post_log.sql  (rate-limit log + triggers)
    4. CLOUDFLARE TURNSTILE: create a free Turnstile widget; copy the Site Key into cabofeira/.env.local as REACT_APP_TURNSTILE_SITE_KEY; copy the Secret Key for the next two steps.
    5. SUPABASE AUTH NATIVE CAPTCHA (signup): Dashboard -> Authentication -> Attack Protection / CAPTCHA -> enable Turnstile, paste the Secret Key.
    6. EDGE FUNCTION (posting CAPTCHA): confirm Edge Functions are available for this project (Open Q1). If yes: `supabase functions deploy verify-turnstile` then `supabase secrets set CLOUDFLARE_SECRET_KEY=<secret>`. If Edge Functions are NOT available, record the decision to use the pg_net fallback (verify pg_net is enabled) and note it for Plan 02-07 — do NOT block the phase, but the posting-CAPTCHA mechanism in 02-07 must match what was deployed here.
    7. TWO-ACCOUNT TRIGGER SANITY (Pitfall 3 / Assumption A2): from two real accounts, send a message A->B and confirm a row appears in public.notifications for B (the notify_new_message definer trigger satisfies create_notification's cross-user gate). If it errors, flag — the trigger fn owner may need admin-equivalence.
  </how-to-verify>
  <resume-signal>Type "applied" once all six SQL files ran clean, Turnstile keys are provisioned, the Auth toggle is on, the Edge Function is deployed (or the pg_net fallback is recorded), and the two-account notification test passed — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → live Supabase | Manual SQL apply + dashboard config; integrity depends on applying files in order |
| developer → Cloudflare/Supabase secrets | Secret key must enter only server-side stores (Auth provider config + Edge secret), never the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm supply chain (@marsidev/react-turnstile [ASSUMED]) | mitigate | blocking-human legitimacy confirm at npmjs.com before install (never auto-approved) |
| T-02-07 | Information Disclosure | secret key placed in .env.local / REACT_APP_* | mitigate | .env.example documents site key only; secret goes to Supabase Auth config + `supabase secrets set` |
| T-02-08 | Tampering | SQL applied out of order (is_blocked_pair missing) | mitigate | checkpoint enforces strict apply order a..f |
| T-02-09 | Repudiation | two-account notification gate untested (A2) | mitigate | checkpoint includes the explicit two-account create_notification test |
</threat_model>

<verification>
- All six SQL files applied clean in order.
- REACT_APP_TURNSTILE_SITE_KEY present in env; CLOUDFLARE_SECRET_KEY only in server-side stores.
- Edge Function deployed OR pg_net fallback recorded for 02-07.
- Native Auth CAPTCHA enabled.
- Two-account message produces a notification row.
- @marsidev/react-turnstile@1.5.2 installed.
</verification>

<success_criteria>
The live backend carries every Phase-2 schema change, CAPTCHA infrastructure is provisioned and toggled, the Turnstile widget dependency is installed, and the new-message notification trigger is proven with two real accounts — unblocking all downstream UI plans.
</success_criteria>

<output>
Create `.planning/phases/02-missing-table-stakes-features/02-02-SUMMARY.md` when done. Record the posting-CAPTCHA mechanism chosen (Edge Function vs pg_net) so Plan 02-07 matches it.
</output>
