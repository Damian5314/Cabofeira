---
phase: 02-missing-table-stakes-features
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - cabofeira/src/pages/ProductDetail.jsx
  - cabofeira/src/pages/Profile.jsx
  - cabofeira/src/index.css
  - cabofeira/src/i18n/en.json
  - cabofeira/src/i18n/pt-cv.json
autonomous: true
requirements: [FEAT-02, FEAT-03, FEAT-04, FEAT-06, FEAT-07]
must_haves:
  truths:
    - "A buyer can contact a seller via a prefilled wa.me WhatsApp button when the seller has a phone (FEAT-06/D-23)"
    - "A buyer can share a listing via native share, WhatsApp, and copy-link (FEAT-07/D-24)"
    - "Reporting uses structured enum reasons stored as the enum key, and a duplicate report is silently deduped (FEAT-02/FEAT-03, D-20/D-21)"
    - "The verified badge renders on the seller profile (FEAT-04); the sold badge renders on product detail"
  artifacts:
    - path: "cabofeira/src/pages/ProductDetail.jsx"
      provides: "WhatsApp button + extended share + structured report modal + sold badge"
      contains: "wa.me"
    - path: "cabofeira/src/pages/Profile.jsx"
      provides: "verified badge next to seller name + public ad list including sold"
      contains: "badge-verified"
  key_links:
    - from: "cabofeira/src/pages/ProductDetail.jsx report modal"
      to: "reports table"
      via: "upsert onConflict reporter_id,product_id ignoreDuplicates"
      pattern: "upsert"
    - from: "cabofeira/src/pages/ProductDetail.jsx"
      to: "wa.me deep link"
      via: "https://wa.me/<phone>?text="
      pattern: "wa.me"
---

<objective>
Extend ProductDetail with the WhatsApp contact button (FEAT-06), the enhanced share affordances (FEAT-07), the structured report modal with silent dedup (FEAT-02/FEAT-03), and the sold badge; extend the seller Profile with the verified badge (FEAT-04) and a public ad list that includes sold listings (D-13).

Purpose: ProductDetail is the contact-the-seller hub and the report entry point; these are the trust-and-completeness features a buyer expects at first visit. The report modal currently stores free-text labels — switch to enum keys (matching reports_reason.sql applied in 02-02) and dedup via upsert.
Output: ProductDetail WhatsApp/share/report/sold-badge; Profile verified badge + sold-inclusive public list; bilingual strings; any new CSS.
</objective>

<execution_context>
@E:/Projecten/Cabofeira/.claude/gsd-core/workflows/execute-plan.md
@E:/Projecten/Cabofeira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-missing-table-stakes-features/02-CONTEXT.md
@.planning/phases/02-missing-table-stakes-features/02-RESEARCH.md
@.planning/phases/02-missing-table-stakes-features/02-PATTERNS.md
@.planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductDetail WhatsApp button + extended share + sold badge</name>
  <files>cabofeira/src/pages/ProductDetail.jsx, cabofeira/src/index.css, cabofeira/src/i18n/en.json, cabofeira/src/i18n/pt-cv.json</files>
  <read_first>
    - cabofeira/src/pages/ProductDetail.jsx (share L167-179; contact-button group L254-275 with the product.seller.phone gate; detail-tags row L230-240; verified badge L310-314)
    - .planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md (§5 WhatsApp+Share, §1 sold badge on detail, §Color WhatsApp green exception)
    - .planning/phases/02-missing-table-stakes-features/02-PATTERNS.md (ProductDetail assignment)
  </read_first>
  <action>
    Per FEAT-06 / D-23, FEAT-07 / D-24, FEAT-01 (detail sold badge).
    1. WhatsApp button (FEAT-06): add a button in the contact .action-row, rendered ONLY when product.seller.phone (mirror the existing phone gate at L254). Style with WhatsApp brand green #25D366 bg + white FaWhatsapp (react-icons/fa) + t("product.contactWhatsapp"); adopt the compact .action-row .btn padding (8px 6px). Link to https://wa.me/<digits-of-seller.phone>?text=<encodeURIComponent(title + " " + window.location.href)> with target="_blank" rel="noopener noreferrer". Strip non-digits from the phone for the wa.me path.
    2. Share (FEAT-07): keep the native navigator.share branch in share() (L167-179). Add an explicit fallback button row (reuse .btn-outline): "Share on WhatsApp" (FaWhatsapp -> https://wa.me/?text=<encodeURIComponent(title + " " + url)>) and "Copy link" (FiLink from react-icons/fi -> navigator.clipboard.writeText(url) -> toast.success(t("product.linkCopied")) which already exists). Use keys product.share / product.shareWhatsapp / product.copyLink.
    3. Sold badge on detail: add {product.status === "sold" && <span className="badge badge-sold">{t("badge.sold")}</span>} to the .detail-tags row (L230-240). Reuse the .badge-sold class added in Plan 02-03 (do not redefine it; if index.css needs no change here, leave it untouched).
    Add bilingual keys to BOTH en.json and pt-cv.json: product.contactWhatsapp ("Chat on WhatsApp"), product.share ("Share"), product.shareWhatsapp ("Share on WhatsApp"), product.copyLink ("Copy link"). Reuse existing product.linkCopied / badge.sold.
  </action>
  <verify>
    <automated>cd cabofeira && npm run build; grep "wa.me" cabofeira/src/pages/ProductDetail.jsx (>=2 occurrences: contact + share); grep "contactWhatsapp" cabofeira/src/i18n/en.json cabofeira/src/i18n/pt-cv.json.</automated>
    <human-check>WhatsApp button shows only when the seller has a phone and opens wa.me prefilled with title+URL; Share offers native/WhatsApp/copy-link with a "Link copied" toast; the sold badge shows on a sold detail page. EN + pt-CV resolve.</human-check>
  </verify>
  <acceptance_criteria>WhatsApp contact button (phone-gated, brand green, prefilled wa.me); share row with WhatsApp + copy-link fallback; sold badge on detail; all new strings in both dictionaries.</acceptance_criteria>
  <done>Contact/share/sold-badge work; bilingual strings present.</done>
</task>

<task type="auto">
  <name>Task 2: Structured report modal with silent dedup</name>
  <files>cabofeira/src/pages/ProductDetail.jsx, cabofeira/src/i18n/en.json, cabofeira/src/i18n/pt-cv.json</files>
  <read_first>
    - cabofeira/src/pages/ProductDetail.jsx (report modal L384-469; report select + insert L416-450; Modal shell L475-493)
    - .planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md (§6 enum table + i18n keys, silent-dedup behavior)
    - .planning/phases/02-missing-table-stakes-features/02-RESEARCH.md (FEAT-02/03 row; D-21 dedup)
  </read_first>
  <action>
    Per FEAT-02 / D-20, FEAT-03 / D-21 (reports_reason.sql + the (reporter_id,product_id) unique constraint were applied in 02-02).
    1. Replace the current free-text/label-storing <select> (or option set) in the report modal with a structured choice where the STORED value is the enum key — render a radio-group (preferred, UI-SPEC §6) or <option value="scam">…</option> mapping. The six values: scam, spam, prohibited, already_sold, duplicate, other. Display labels come from t("report.reason.scam") etc. (keys: report.reason.scam/spam/prohibited/alreadySold/duplicate/other). Keep the optional free-text details textarea. Submit button = .btn-primary t("report.submit"). Selected radio uses the --cf-accent ring.
    2. Change the insert to a dedup-safe upsert: supabase.from("reports").upsert({ product_id, reporter_id: user.id, reason: reportReason, details }, { onConflict: "reporter_id,product_id", ignoreDuplicates: true }). A duplicate is SILENTLY deduped (D-21) — never a hard error. On a detected already-reported state, show a calm t("report.alreadyReported") confirmation/disabled state, NOT the report-error box.
    Add bilingual keys to BOTH en.json and pt-cv.json: report.reason.{scam,spam,prohibited,alreadySold,duplicate,other}, report.submit, report.alreadyReported — EN copy verbatim from UI-SPEC §6 / §Copywriting; provide pt-CV.
  </action>
  <verify>
    <automated>cd cabofeira && npm run build; grep "upsert" cabofeira/src/pages/ProductDetail.jsx; grep "onConflict" cabofeira/src/pages/ProductDetail.jsx; grep "report.reason.scam\|report.reason" cabofeira/src/i18n/en.json cabofeira/src/i18n/pt-cv.json.</automated>
    <human-check>The report modal offers the six structured reasons (stored as enum keys), submits via upsert, and a second report on the same listing succeeds silently with a calm "already reported" state — never a hard error. EN + pt-CV resolve.</human-check>
  </verify>
  <acceptance_criteria>Report modal stores enum keys, dedups silently via upsert(onConflict, ignoreDuplicates), and shows a calm already-reported state; all report.* keys in both dictionaries.</acceptance_criteria>
  <done>Structured + deduped reporting works; bilingual strings present.</done>
</task>

<task type="auto">
  <name>Task 3: Profile verified badge + sold-inclusive public ad list</name>
  <files>cabofeira/src/pages/Profile.jsx, cabofeira/src/i18n/en.json, cabofeira/src/i18n/pt-cv.json</files>
  <read_first>
    - cabofeira/src/pages/Profile.jsx (seller name/header area; the public ad list query)
    - cabofeira/src/pages/ProductDetail.jsx (verified badge L310-314 — reuse exact class)
    - cabofeira/src/context/ProductsContext.jsx (fetchProducts status option added in 02-03)
    - .planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md (§2 verified placement on profile; §1 sold ads visible on public profile)
  </read_first>
  <action>
    Per FEAT-04 (profile surface) + FEAT-01 / D-13.
    1. Render {seller.verified && <span className="badge badge-verified" title={t("badge.verified")}>{t("badge.verified")}</span>} next to the seller name in Profile (reuse the .seller-name inline 6px gap pattern; reuse the existing badge.verified key — do not enlarge or restyle).
    2. The seller's PUBLIC profile ad list should include sold listings (with the Sold badge) but still exclude expired/hidden — call fetchProducts({ sellerId, status: ["active","sold"] }) (the status option was added in 02-03). The owner's own My Ads view is unchanged. Ensure the ProductCard sold badge (from 02-03) renders here.
    Add no new strings beyond reusing badge.verified / badge.sold (already added in 02-03); if Profile needs an "ads" empty-state string and one does not exist, reuse the existing profile/empty key.
  </action>
  <verify>
    <automated>cd cabofeira && npm run build; grep "badge-verified" cabofeira/src/pages/Profile.jsx; grep "status.*sold\|\\[\"active\", \"sold\"\\]\|active.*sold" cabofeira/src/pages/Profile.jsx.</automated>
    <human-check>A verified seller's profile shows the verified badge by the name; the public profile lists their active AND sold ads (sold badged) but not expired/hidden.</human-check>
  </verify>
  <acceptance_criteria>Profile renders the verified badge and a public ad list including sold (badged) but not expired/hidden via fetchProducts status override.</acceptance_criteria>
  <done>Profile verified badge + sold-inclusive public list work; build compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client report insert → Postgres | reports CHECK + (reporter_id,product_id) unique enforce valid reason + dedup server-side; client upsert is convenience |
| rendered report details / message body | user-supplied text rendered in admin/report views |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | report with an out-of-set reason | mitigate | DB CHECK (reports_reason.sql) rejects invalid reasons; UI only offers the six enum keys |
| T-02-12 | DoS/Repudiation | duplicate report spam | mitigate | unique(reporter_id,product_id) + ignoreDuplicates upsert dedups silently (D-21) |
| T-02-13 | XSS | report details / title rendered | mitigate | React escapes by default; no dangerouslySetInnerHTML introduced |
| T-02-14 | Information Disclosure | wa.me link leaking seller phone | accept | phone is already shown via the existing phone contact button; WhatsApp gate matches existing disclosure |
</threat_model>

<verification>
- WhatsApp button phone-gated + prefilled; share offers native/WhatsApp/copy-link.
- Report modal stores enum keys + dedups silently via upsert.
- Sold badge on detail; verified badge on profile; public profile lists active+sold only.
- All new strings (product.*, report.*) in BOTH dictionaries.
- CRA build compiles.
</verification>

<success_criteria>
A buyer can WhatsApp/share a listing and file a structured, deduped report; the seller profile shows the verified badge and the seller's active+sold ads — all bilingual.
</success_criteria>

<output>
Create `.planning/phases/02-missing-table-stakes-features/02-04-SUMMARY.md` when done.
</output>
