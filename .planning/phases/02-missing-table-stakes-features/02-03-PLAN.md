---
phase: 02-missing-table-stakes-features
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - cabofeira/src/context/ProductsContext.jsx
  - cabofeira/src/pages/MyAds.jsx
  - cabofeira/src/components/ProductCard.jsx
  - cabofeira/src/index.css
  - cabofeira/src/i18n/en.json
  - cabofeira/src/i18n/pt-cv.json
autonomous: true
requirements: [FEAT-01, FEAT-04]
must_haves:
  truths:
    - "A seller can mark a listing sold (and back to active) from My Ads via ConfirmDialog (D-11/D-12)"
    - "Sold listings leave Home and Search (app filters status='active', paired with the D-14 SELECT relax)"
    - "A 'Sold' badge renders on product cards (D-15)"
    - "A verified-seller badge renders on product cards (FEAT-04 / D-25)"
  artifacts:
    - path: "cabofeira/src/index.css"
      provides: ".badge-sold class matching .badge geometry"
      contains: ".badge-sold"
    - path: "cabofeira/src/pages/MyAds.jsx"
      provides: "mark-as-sold / mark-as-active toggle control"
      contains: "markSold"
    - path: "cabofeira/src/context/ProductsContext.jsx"
      provides: "status passthrough in updateProduct + active-only feed/search"
      contains: "status"
  key_links:
    - from: "cabofeira/src/pages/MyAds.jsx"
      to: "ProductsContext.updateProduct"
      via: "updateProduct(id,{status})"
      pattern: "updateProduct"
    - from: "cabofeira/src/context/ProductsContext.jsx"
      to: "products query"
      via: ".eq('status','active')"
      pattern: "status.*active"
---

<objective>
Deliver mark-as-sold (FEAT-01) and the verified/sold badges on product cards (FEAT-04 / D-25 card surface, FEAT-01 badge), and close the sold-leak window by adding the app-level active-only filter that pairs with the Plan-02-02 SELECT relax (D-14, Pitfall 1).

Purpose: After products_sold_visibility.sql relaxed the SELECT policy to expose sold listings publicly, the feed/search queries MUST filter status='active' or sold ads leak into Home/Search (Pitfall 1). This plan ships that filter together with the seller's mark-as-sold control and the card badges.
Output: ProductsContext status passthrough + active-only feed/search; MyAds mark-sold toggle; ProductCard sold + verified badges; .badge-sold CSS; bilingual strings.
</objective>

<execution_context>
@E:/Projecten/Cabofeira/.claude/gsd-core/workflows/execute-plan.md
@E:/Projecten/Cabofeira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-missing-table-stakes-features/02-CONTEXT.md
@.planning/phases/02-missing-table-stakes-features/02-RESEARCH.md
@.planning/phases/02-missing-table-stakes-features/02-PATTERNS.md
@.planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md
@cabofeira/src/context/ProductsContext.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductsContext — status passthrough + active-only feed/search</name>
  <files>cabofeira/src/context/ProductsContext.jsx</files>
  <read_first>
    - cabofeira/src/context/ProductsContext.jsx (updateProduct ~L195-220 dbPatch build; refreshProducts L82-86; fetchProducts L110-160; toggleFavorite L250-265 optimistic pattern)
    - .planning/phases/02-missing-table-stakes-features/02-RESEARCH.md (Pitfall 1)
    - .planning/phases/02-missing-table-stakes-features/02-PATTERNS.md (ProductsContext assignment)
  </read_first>
  <action>
    Per FEAT-01 / D-11/D-12/D-14 (paired with products_sold_visibility.sql applied in 02-02).
    1. In updateProduct (~L195-209), where dbPatch is assembled, add: if ("status" in patch) dbPatch.status = patch.status; — so updateProduct(id,{status:'sold'}) and {status:'active'} round-trip (the Phase-1 guard_products_update does NOT pin status, so this is allowed).
    2. In refreshProducts (L82-86), add .eq("status","active") to the cached-pool query (Pitfall 1 — required now that RLS permits sold).
    3. In fetchProducts (L110-160), add a status option defaulting to "active": when opts.status is an array use .in("status", opts.status); when a string use .eq("status", opts.status); default .eq("status","active"). This lets Search stay active-only while the seller's PUBLIC profile/ad list (Plan 02-04) can pass status:["active","sold"] to show sold ads with a badge (D-13).
    Keep the {ok,error} / throw conventions already in the file. Add a // eslint-disable-next-line no-console before any console.error using the [products] prefix.
  </action>
  <verify>
    <automated>cd cabofeira && npm run build (CRA build compiles); grep -c "status.*active" cabofeira/src/context/ProductsContext.jsx returns >=2 (refreshProducts + fetchProducts default).</automated>
    <human-check>Confirm updateProduct copies status into dbPatch; refreshProducts and fetchProducts default to active-only; fetchProducts accepts a status option for the public-profile sold case.</human-check>
  </verify>
  <acceptance_criteria>updateProduct passes status through; Home cache and Search default to status='active'; fetchProducts accepts a status override (array/string) for the public-profile sold list.</acceptance_criteria>
  <done>Status round-trips through updateProduct; feed/search are active-only by default; build compiles.</done>
</task>

<task type="auto">
  <name>Task 2: MyAds mark-as-sold control (ConfirmDialog toggle)</name>
  <files>cabofeira/src/pages/MyAds.jsx, cabofeira/src/i18n/en.json, cabofeira/src/i18n/pt-cv.json</files>
  <read_first>
    - cabofeira/src/pages/MyAds.jsx (~L19-87 per-ad action area)
    - cabofeira/src/pages/Admin.jsx (askConfirm + ConfirmDialog delete flow L432-445 — the analog)
    - cabofeira/src/components/ConfirmDialog.jsx (danger/busy props)
    - cabofeira/src/context/ProductsContext.jsx (updateProduct, toggleFavorite optimistic pattern)
    - .planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md (§1, §Destructive confirmations — mark-sold is danger=false/reversible)
  </read_first>
  <action>
    Per FEAT-01 / D-11/D-12. Add a per-ad button next to the existing edit/delete in MyAds, labelled t("myAds.markSold") when status!=='sold' and t("myAds.markActive") when status==='sold'. On click open ConfirmDialog with danger=false (reversible — NOT destructive, no requireText): for sold use title myAds.markSoldTitle / body myAds.markSoldBody / confirm "Mark as sold"; for active use myAds.markActiveTitle / markActiveBody. The confirm action calls updateProduct(id, { status: status === "sold" ? "active" : "sold" }) and updates local UI (mirror the toggleFavorite optimistic pattern; on error surface the existing toast error). Reuse the existing ConfirmDialog state machinery in MyAds if present, else mirror Admin.jsx askConfirm.

    Add these bilingual keys to BOTH en.json and pt-cv.json (nested namespaces, English fallback): myAds.markSold, myAds.markActive, myAds.markSoldTitle, myAds.markSoldBody, myAds.markActiveTitle, myAds.markActiveBody — EN copy verbatim from UI-SPEC §Destructive confirmations; provide the pt-CV translations.
  </action>
  <verify>
    <automated>cd cabofeira && npm run build; grep "myAds.markSold" cabofeira/src/i18n/en.json cabofeira/src/i18n/pt-cv.json (present in both); grep "markSold\|markActive" cabofeira/src/pages/MyAds.jsx.</automated>
    <human-check>From My Ads, marking an ad sold opens a non-danger confirm, flips the badge/label, removes it from Home/Search but keeps it on the seller's public profile; marking active reverses it. EN + pt-CV labels both resolve.</human-check>
  </verify>
  <acceptance_criteria>My Ads shows a reversible mark-sold/mark-active toggle using ConfirmDialog (danger=false) wired to updateProduct({status}); all six new keys exist in both dictionaries.</acceptance_criteria>
  <done>Mark-as-sold toggle works optimistically; bilingual strings present in both files.</done>
</task>

<task type="auto">
  <name>Task 3: ProductCard sold + verified badges + .badge-sold CSS</name>
  <files>cabofeira/src/components/ProductCard.jsx, cabofeira/src/index.css, cabofeira/src/i18n/en.json, cabofeira/src/i18n/pt-cv.json</files>
  <read_first>
    - cabofeira/src/components/ProductCard.jsx (featured badge L17-19, card-image-wrap badge slot, seller/meta line)
    - cabofeira/src/pages/ProductDetail.jsx (verified badge L310-314 — reuse exact class)
    - cabofeira/src/index.css (.badge L165, .badge-verified L180-183, .badge-featured L175-178)
    - .planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md (§1 badge slot/stacking, §2 verified placement, §Color .badge-sold rule)
  </read_first>
  <action>
    Per FEAT-01 (sold badge) + FEAT-04 / D-25 (verified badge extended to cards; seller.verified already in PRODUCT_SELECT/fromRow — pure presentation, no query change).
    1. index.css: add .badge-sold with geometry identical to .badge (padding:2px 8px; border-radius:999px; font-size:0.72rem; font-weight:600). Use a muted/neutral or red-tint fill that is NOT yellow (reserved for featured) and NOT success-green — per UI-SPEC §Color pick ONE: either background:#e5e7eb;color:#374151 OR the "no longer available" red-tint background:#fee2e2;color:#991b1b. Apply the same choice on card + detail (detail badge is Plan 02-04). Reuse .badge-verified UNCHANGED.
    2. ProductCard.jsx: add {product.status === "sold" && <span className="badge badge-sold">{t("badge.sold")}</span>} in the .card-image-wrap badge slot (same absolute slot as featured; if both present, stack 8px with sold winning priority — UI-SPEC §1). Add the verified pill near the seller/meta line: {product.seller.verified && <span className="badge badge-verified" title={t("badge.verified")}>{t("badge.verified")}</span>} — do NOT enlarge at card scale (UI-SPEC §2). product.status and product.seller.verified already flow via fromRow — no query change.
    3. Add badge.sold to BOTH en.json and pt-cv.json (EN "Sold"). Reuse the existing badge.verified key (do not duplicate); if ProductCard needs the verified label and the existing key differs (e.g. product.verified), use the existing one consistently with ProductDetail.
  </action>
  <verify>
    <automated>cd cabofeira && npm run build; grep ".badge-sold" cabofeira/src/index.css; grep "badge.sold\|badge-sold" cabofeira/src/components/ProductCard.jsx; grep "\"sold\"" cabofeira/src/i18n/en.json cabofeira/src/i18n/pt-cv.json.</automated>
    <human-check>A sold card shows the Sold badge in the badge slot (sold over featured when both); a verified seller's card shows the verified pill at natural size; .badge-sold is not yellow/green.</human-check>
  </verify>
  <acceptance_criteria>.badge-sold exists with .badge geometry and a non-reserved color; ProductCard renders sold + verified badges from already-flowing data; badge.sold present in both dictionaries.</acceptance_criteria>
  <done>Card badges render; CSS class added; bilingual sold label present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client feed query → Postgres | After the SELECT relax, the app filter is what keeps sold ads out of Home/Search (UX, not the security boundary — RLS already excludes hidden/expired) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | sold ads leaking into Home/Search after the D-14 relax | mitigate | refreshProducts + fetchProducts default .eq('status','active') (Pitfall 1); shipped in the same plan as the relax-dependent UI |
| T-02-10 | Tampering | non-owner flipping status via updateProduct | accept | Phase-1 products UPDATE guard + RLS with-check (auth.uid()=seller_id) already restrict updates to the owner; this plan only adds status passthrough on an already-guarded path |
</threat_model>

<verification>
- updateProduct passes status; feed/search default to active.
- MyAds toggle marks sold/active via a reversible ConfirmDialog.
- ProductCard shows sold + verified badges; .badge-sold added (not yellow/green).
- badge.sold + myAds.* keys exist in BOTH en.json and pt-cv.json.
- CRA build compiles.
</verification>

<success_criteria>
A seller marks a listing sold from My Ads; it disappears from Home/Search but shows a Sold badge on cards and the public profile; verified sellers show a verified pill on cards — all strings bilingual.
</success_criteria>

<output>
Create `.planning/phases/02-missing-table-stakes-features/02-03-SUMMARY.md` when done.
</output>
