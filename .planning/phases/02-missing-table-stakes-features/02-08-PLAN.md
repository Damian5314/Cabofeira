---
phase: 02-missing-table-stakes-features
plan: 08
type: execute
wave: 8
depends_on: ["02-04", "02-07"]
files_modified:
  - cabofeira/src/context/AuthContext.jsx
  - cabofeira/src/context/PricingContext.jsx
  - cabofeira/src/pages/Admin.jsx
  - cabofeira/src/i18n/en.json
  - cabofeira/src/i18n/pt-cv.json
autonomous: true
requirements: [ADMIN-01, ADMIN-02, FEAT-02]
must_haves:
  truths:
    - "Every admin mutation (delete ad, change role, verify user, change prices) is recorded via log_admin_action with before/after details (ADMIN-01, D-26)"
    - "An admin can view a read-only audit-log tab in the admin panel with filter + pagination (ADMIN-02, D-27)"
    - "An admin can filter the reports tab by structured reason (FEAT-02, D-22)"
  artifacts:
    - path: "cabofeira/src/pages/Admin.jsx"
      provides: "audit tab + reports reason filter + delete-ad audit write"
      contains: "log_admin_action"
    - path: "cabofeira/src/context/AuthContext.jsx"
      provides: "audit writes on setUserRole + setUserVerified"
      contains: "log_admin_action"
    - path: "cabofeira/src/context/PricingContext.jsx"
      provides: "audit write on persisted price change"
      contains: "log_admin_action"
  key_links:
    - from: "cabofeira/src/pages/Admin.jsx audit tab"
      to: "admin_audit_log table"
      via: "select order created_at desc + range pagination"
      pattern: "admin_audit_log"
    - from: "admin mutation callers"
      to: "log_admin_action RPC"
      via: "supabase.rpc('log_admin_action', ...)"
      pattern: "log_admin_action"
---

<objective>
Record every admin mutation in the Phase-1 audit log (ADMIN-01) and add the read-only audit-log viewer tab plus a reports-by-reason filter to the admin panel (ADMIN-02, FEAT-02 admin half). All server-side primitives already exist (log_admin_action RPC + admin-only SELECT RLS from Phase 1; reports.reason from 02-02) — this is code-only wiring.

Purpose: Admin accountability is a launch requirement. log_admin_action is append-only and admin-gated; callers serialize before/after into details (D-12/D-26). The viewer reads an already-admin-restricted table (no new SQL).
Output: audit writes in AuthContext (role/verify) + PricingContext (price) + Admin.jsx (delete ad); a new audit tab; a reports reason filter; bilingual strings.
</objective>

<execution_context>
@E:/Projecten/Cabofeira/.claude/gsd-core/workflows/execute-plan.md
@E:/Projecten/Cabofeira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-missing-table-stakes-features/02-CONTEXT.md
@.planning/phases/02-missing-table-stakes-features/02-RESEARCH.md
@.planning/phases/02-missing-table-stakes-features/02-PATTERNS.md
@.planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md
@cabofeira/supabase/admin_audit_log.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit writes on role / verify / price mutations</name>
  <files>cabofeira/src/context/AuthContext.jsx, cabofeira/src/context/PricingContext.jsx</files>
  <read_first>
    - cabofeira/src/context/AuthContext.jsx (setUserRole L215-228, setUserVerified L230-240 — both already isAdmin-gated; read prior value before mutating)
    - cabofeira/src/context/PricingContext.jsx (persistPrices L75-87, persistFeatured, setPrice L106-112 debounced)
    - cabofeira/supabase/admin_audit_log.sql (log_admin_action(p_action,p_target_table,p_target_id,p_details) signature + in-fn is_admin gate)
    - .planning/phases/02-missing-table-stakes-features/02-RESEARCH.md (§"Audit-log wiring"; D-26/D-12; the removeProduct shared-by-sellers caveat)
    - .planning/phases/02-missing-table-stakes-features/02-PATTERNS.md (AuthContext / PricingContext / Shared "Audit-write wiring" assignments)
  </read_first>
  <action>
    Per ADMIN-01 / D-26/D-12. Capture the previous value BEFORE each mutation, then after a successful update call the RPC:
    1. AuthContext.setUserRole (L215-228): before updating, read prevRole (from users state or the returned row); after success, await supabase.rpc("log_admin_action", { p_action: "change_role", p_target_table: "profiles", p_target_id: userId, p_details: { old_role: prevRole, new_role: role } }).
    2. AuthContext.setUserVerified (L230-240): same shape with p_action:"verify_user", p_details:{ old: prevVerified, new: verified }.
    3. PricingContext: wire log_admin_action("change_price","app_settings", null, { category, old, new }) on the PERSISTED write (inside persistPrices/persistFeatured after the successful upsert — NOT on every debounced keystroke). Capture old from the prior prices state.
    Only call log_admin_action from admin-gated paths (setUserRole/setUserVerified are already isAdmin-gated; pricing persist is admin-only). The RPC has its own in-fn is_admin() gate. Wrap each rpc call so a logging failure does not break the primary mutation (log the error with the [auth]/[pricing] prefix; do not throw).
  </action>
  <verify>
    <automated>cd cabofeira && npm run build; grep -c "log_admin_action" cabofeira/src/context/AuthContext.jsx returns >=2; grep "log_admin_action\|change_price" cabofeira/src/context/PricingContext.jsx.</automated>
    <human-check>Changing a role/verify flag and changing a price each create an admin_audit_log row with correct old/new details; a logging error does not abort the mutation.</human-check>
  </verify>
  <acceptance_criteria>setUserRole (change_role), setUserVerified (verify_user), and persisted price changes (change_price) each log before/after details via log_admin_action from admin-gated paths only.</acceptance_criteria>
  <done>Three audit-write call sites wired with before/after details; build compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Admin audit-log viewer tab + delete-ad audit write</name>
  <files>cabofeira/src/pages/Admin.jsx, cabofeira/src/i18n/en.json, cabofeira/src/i18n/pt-cv.json</files>
  <read_first>
    - cabofeira/src/pages/Admin.jsx (tab bar L179-204; loadReports L44-70; loadAds pagination L89-106; delete-ad flow L432-445/L438-443; timeAgo usage)
    - cabofeira/supabase/admin_audit_log.sql (row shape: actor_id, action, target_table, target_id, details, created_at; admin-only SELECT RLS)
    - .planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md (§7 audit tab columns/filter/pagination/empty states)
    - .planning/phases/02-missing-table-stakes-features/02-RESEARCH.md (§"Audit-log wiring" delete_ad + removeProduct shared-by-sellers caveat)
  </read_first>
  <action>
    Per ADMIN-02 / D-27 + ADMIN-01 delete-ad.
    1. New "Audit log" tab: extend the .admin-tabs bar (reuse button / .is-active blue-underline). A loadAudit callback mirroring loadReports (L44-54) queries admin_audit_log select(action,target_table,target_id,details,created_at, actor:profiles!...(name)) order by created_at desc with range() pagination (mirror loadAds L89-106; or simple Prev/Next .btn-outline if no pagination helper exists). Render a read-only .admin-table with columns Action · Target · Actor · Timestamp (timeAgo), header keys admin.audit.action/.target/.actor/.time. A <select> filter by action in the .row-between header. Empty/filtered-empty states: admin.auditEmpty / admin.auditFilterEmpty. No row actions (RLS already admin-only SELECT).
    2. Delete-ad audit write: in the admin delete flow (~L438-443), after removeProduct(p.id) succeeds AND only when isAdmin, also call supabase.rpc("log_admin_action", { p_action: "delete_ad", p_target_table: "products", p_target_id: p.id, p_details: { title: p.title } }). CAVEAT (RESEARCH): removeProduct is shared by sellers — guard the rpc call behind isAdmin so non-admin sellers do not hit the admin-gated RPC and error.
    Add bilingual keys to BOTH en.json and pt-cv.json: admin.audit.action/.target/.actor/.time, admin.auditEmpty, admin.auditFilterEmpty — EN verbatim from UI-SPEC §Copywriting; provide pt-CV.
  </action>
  <verify>
    <automated>cd cabofeira && npm run build; grep "admin_audit_log\|loadAudit" cabofeira/src/pages/Admin.jsx; grep "delete_ad" cabofeira/src/pages/Admin.jsx; grep "admin.audit.action\|auditEmpty" cabofeira/src/i18n/en.json cabofeira/src/i18n/pt-cv.json.</automated>
    <human-check>The Audit log tab lists actions (Action/Target/Actor/Timestamp) with action filter + pagination + empty states; deleting an ad as admin records a delete_ad row; a non-admin seller deleting their own ad does NOT error.</human-check>
  </verify>
  <acceptance_criteria>A read-only admin audit tab (filter + pagination + empty states) reads admin_audit_log; admin ad-deletion logs delete_ad guarded by isAdmin; audit strings in both dictionaries.</acceptance_criteria>
  <done>Audit viewer + delete-ad write wired; bilingual strings present.</done>
</task>

<task type="auto">
  <name>Task 3: Reports tab filter-by-reason</name>
  <files>cabofeira/src/pages/Admin.jsx, cabofeira/src/i18n/en.json, cabofeira/src/i18n/pt-cv.json</files>
  <read_first>
    - cabofeira/src/pages/Admin.jsx (reports tab + loadReports L44-70; .row-between header filter placement)
    - .planning/phases/02-missing-table-stakes-features/02-UI-SPEC.md (§7 reason filter; §6 the six report.reason.* keys)
  </read_first>
  <action>
    Per FEAT-02 / D-22. Add a reason <select> filter to the reports tab header (styled to match existing admin filters in .row-between), with options = "All reasons" (admin.reportsAllReasons) + the six report.reason.* labels (reuse the keys added in 02-04: scam/spam/prohibited/alreadySold/duplicate/other). Filter the loaded reports client-side by the selected enum value (or pass .eq("reason", value) to the query). When the filter yields no rows, show admin.reportsFilterEmpty.
    Add bilingual keys to BOTH en.json and pt-cv.json: admin.reportsAllReasons ("All reasons"), admin.reportsFilterEmpty — EN verbatim from UI-SPEC; provide pt-CV. (report.reason.* already exist from 02-04 — reuse, do not duplicate.)
  </action>
  <verify>
    <automated>cd cabofeira && npm run build; grep "reportsAllReasons\|reason.*filter\|reportReasonFilter" cabofeira/src/pages/Admin.jsx; grep "reportsAllReasons\|reportsFilterEmpty" cabofeira/src/i18n/en.json cabofeira/src/i18n/pt-cv.json.</automated>
    <human-check>The reports tab offers an "All reasons" + six-reason filter that narrows the list and shows the filtered-empty state when nothing matches. EN + pt-CV resolve.</human-check>
  </verify>
  <acceptance_criteria>Reports tab has a by-reason filter (All + six enum labels) with a filtered-empty state, reusing the report.reason.* keys; new admin.reports* keys in both dictionaries.</acceptance_criteria>
  <done>Reports reason filter works; bilingual strings present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → log_admin_action | RPC has an in-fn is_admin() gate; append-only via RLS; callers must be admin-gated |
| admin client → admin_audit_log SELECT | RLS restricts SELECT to admins (Phase 1) — the viewer reads only as admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Tampering/Repudiation | non-admin forging audit rows | mitigate | log_admin_action in-fn is_admin() gate + append-only RLS; callers gated behind isAdmin (delete_ad guarded so sellers don't hit it) |
| T-02-22 | Information Disclosure | non-admin reading the audit log | mitigate | admin_audit_log SELECT RLS admin-only (Phase 1); viewer reads as the authed admin |
| T-02-13 | XSS | rendering audit details / report fields | mitigate | React escapes; details rendered as text (no dangerouslySetInnerHTML) |
| T-02-23 | Repudiation | admin mutation not recorded | mitigate | log_admin_action wired into all four mutation paths with before/after details (ADMIN-01) |
</threat_model>

<verification>
- change_role / verify_user / change_price / delete_ad each produce an audit row with before/after details.
- Audit tab lists rows with filter + pagination + empty states; SELECT is admin-only.
- delete_ad write guarded by isAdmin (sellers don't error).
- Reports tab filters by the six enum reasons + All.
- All admin.* keys in BOTH dictionaries.
- CRA build compiles.
</verification>

<success_criteria>
Every admin mutation is recorded in the append-only audit log, an admin can browse it (filtered, paginated) in the panel, and the reports tab filters by structured reason — all bilingual.
</success_criteria>

<output>
Create `.planning/phases/02-missing-table-stakes-features/02-08-SUMMARY.md` when done.
</output>
