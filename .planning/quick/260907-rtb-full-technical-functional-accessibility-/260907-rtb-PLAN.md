# Full audit

Inspect all runtime source/configuration and SQL, inventory features, run isolated local frontend with synthetic API fixtures (Docker unavailable), record responsive/browser evidence, repair bounded bugs, add regression tests, run lint/test/build and write prioritized report. No live database writes, external settings, deployment, feature removal or destructive tests. Existing changes are the baseline. Larger architecture changes belong in backlog.
